$("#this_memo_<%= @memo.id %>").replaceWith("<%= j render "memos/edit", task_item: @task_item, memo: @memo %>");

$(function(){
  if($(".notice").length){
    $(".notice").html("<%= j flash[:notice] %>");
  } else {
    $("#notice").html("<%= j flash[:notice] %>");
  }
})